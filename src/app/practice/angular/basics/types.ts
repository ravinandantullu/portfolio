// Gives the error free code because of the typings.
// Resolves compilation errors.
let myString: string;
myString = 'Hello world';
// Throughs the error
// myString = 4;

// Other basic types
let string: string;
let aNumber: number;
let aBoolean: boolean;
let aArray: Array<string>;
let aAny: any;
// void, Enum

// Static methods in the class can be used with-out instanciating the class.
